const seed=[{text:"I hope someone out there knows they're not alone.",time:"just now"},{text:"Some days I just want to disappear for a while.",time:"a moment ago"},{text:"You looked really nice today. I didn't have the courage to say it.",time:"2 min ago"},{text:"Maybe tomorrow will feel lighter.",time:"5 min ago"}];const key="anonymous-sky-messages";const saved=JSON.parse(localStorage.getItem(key)||"null");let messages=saved||seed;const grid=document.getElementById("feedGrid"),count=document.getElementById("messageCount"),input=document.getElementById("messageInput"),counter=document.getElementById("counter");function escapeHtml(s){return s.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[c]))}function render(){count.textContent=messages.length;if(!messages.length){grid.innerHTML='<div class="empty">The sky is quiet. Be the first to release a thought.</div>';return}grid.innerHTML=messages.slice().reverse().map(m=>'<article class="card"><p>'+escapeHtml(m.text)+'</p><small>Anonymous · '+escapeHtml(m.time||"now")+'</small></article>').join("")}input.addEventListener("input",()=>counter.textContent=input.value.length+" / 500");document.getElementById("messageForm").addEventListener("submit",e=>{e.preventDefault();const text=input.value.trim();if(!text)return;messages.push({text,time:"just now"});localStorage.setItem(key,JSON.stringify(messages));input.value="";counter.textContent="0 / 500";render();document.getElementById("feed").scrollIntoView({behavior:"smooth"})});document.getElementById("clearLocal").onclick=()=>{localStorage.removeItem(key);messages=[...seed];render()};const music=document.getElementById("bgMusic"),musicBtn=document.getElementById("musicBtn");musicBtn.onclick=async()=>{if(music.paused){try{await music.play();musicBtn.textContent="❚❚"}catch{alert("Add Mass Anasthesia.mp3 to the repository to enable the music.")}}else{music.pause();musicBtn.textContent="♫"}};render();